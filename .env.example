# ---- HTTP API ----
# Shared secret the frontend sends as the x-api-key header.
BACKEND_API_KEY=change-me
# Comma-separated origins allowed to call this service from a browser.
CORS_ORIGINS=http://localhost:5173,https://tg-tools-downlaod.vercel.app
PORT=8000

# ---- Supabase (service role, NEVER the anon key) ----
SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# ---- Telegram userbot ----
# Preferred: keep these here instead of in the database.
TELEGRAM_API_ID=
TELEGRAM_API_HASH=
TELEGRAM_PHONE=
# Filled in after the first login; paste it back here to survive restarts.
TELEGRAM_SESSION_STRING=

# ---- "Log in with Telegram" on the sign-in screen ----
# A separate, lightweight Bot API bot -- unrelated to the userbot above.
# Create one with @BotFather (/newbot), then send /setdomain in the same
# chat pointed at the deployed frontend origin (the widget refuses to
# render anywhere else). Paste its token here.
TELEGRAM_LOGIN_BOT_TOKEN=

# ---- Subscriptions & payments ----
# The chat id the bot above should DM with Approve/Reject buttons whenever a
# subscriber submits a payment claim. Message the bot once from your own
# Telegram account, then check https://api.telegram.org/bot<token>/getUpdates
# for your chat id.
TELEGRAM_ADMIN_CHAT_ID=
# A long random string you make up yourself -- required before
# /api/subscription/aba-ingest accepts anything (fails closed otherwise).
# See ABA_AUTO_CONFIRM_FORWARD_GUIDE for wiring a phone automation app to it.
ABA_INGEST_SECRET=
# The account name ABA's own notification text shows for a payment to you --
# matched literally before an ABA notification is ever trusted.
ABA_MERCHANT_NAME=

# ---- Cloudflare R2 ----
# Optional overrides; by default these are read from the r2_settings table.
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_ENDPOINT_URL=
R2_PUBLIC_URL=
R2_REGION=auto

# ---- Source S3-compatible bucket (one-time import into R2) ----
# Only used by `node src/migrate-s3-to-r2.js` and POST /api/s3import/* to
# pull existing videos into R2 and then delete them from here. Leave blank
# unless you are running that migration.
S3_ENDPOINT=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=
S3_REGION=us-east-1
S3_FORCE_PATH_STYLE=false

# ---- Worker ----
# Seconds between background passes (downloads, auto rules, auto-forward).
WORKER_INTERVAL=30
# How many downloads may run at once (overrides download_settings when set).
# There is no hardcoded ceiling in the code -- set this as high as your CPU,
# disk and network can take. Telegram's own per-account throttle (FLOOD_WAIT)
# is a separate, server-side thing this number cannot raise or lower.
MAX_CONCURRENT_DOWNLOADS=
# Where files are staged before the R2 upload. Windows example: D:\tg-downloads
DOWNLOAD_DIR=
# How many URL-list links are pulled into R2 at once ("Save all to R2"). These
# are plain HTTP transfers with no Telegram limit behind them; two at a time
# keeps one slow host from stalling the rest. Default 2.
MAX_CONCURRENT_URL_FETCHES=2
# Folder in the bucket for videos saved from a URL list. Default "urls".
URL_FETCH_FOLDER=urls
# User-Agent sent with every yt-dlp fetch by ytdlp.js -- some CDNs 403 a
# request with no recognizable browser UA even with a correct Referer.
# Requires yt-dlp + ffmpeg on PATH (already installed by the Dockerfile).
# M3U8_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36
# Path to a Netscape-format cookies.txt, passed to yt-dlp via --cookies for
# sources that only serve the real video to a logged-in session. Optional --
# ytdlp.js runs without cookies until this points at a file that exists.
# YTDLP_COOKIES_FILE=/path/to/cookies.txt

# ---- Speed / rate-limit tuning ----
# teleproto already opens up to 8 parallel connections per download file and
# grows the transfer window on its own. Raise this only if you have a fast
# link and want to push past that default; it trades a slightly higher chance
# of a temporary FLOOD_WAIT for more throughput. Leave blank to use the
# library's default.
TELEGRAM_MAX_DOWNLOAD_SESSIONS=
# Milliseconds the forwarder waits between messages by choice, to stay well
# under Telegram's flood limits on bulk forwarding/mirroring. Any real
# FLOOD_WAIT Telegram returns is always honored in full regardless of this
# value -- lowering it just means fewer of those waits are needed in the
# first place. Default 1500.
FORWARD_PAUSE_MS=1500
# Seconds: teleproto itself sleeps out any FLOOD_WAIT at or under this
# threshold automatically, silently, for EVERY API call the client makes --
# scanning, entity lookups, joins, all of it, not just the download/forward
# calls floodRetry.js wraps. The library's own default is a conservative 60s;
# this raises it so more of the ordinary waits are absorbed without an error
# ever surfacing. Capped by the library itself at 24h. Default here: 300 (5m).
TELEGRAM_FLOOD_SLEEP_THRESHOLD=300
